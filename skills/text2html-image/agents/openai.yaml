interface:
  display_name: "text2html-image"
  short_description: "HTML/CSS ecommerce image pipeline"
  default_prompt: "Use $text2html-image to generate, validate, localize, and export editable ecommerce poster HTML."
